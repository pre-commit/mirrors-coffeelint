{
    "name": "__dummy_package",
    "description": "Note: double curly-braces because python .format",
    "version": "0.0.0",
    "scripts": {
        "preinstall": "npm install coffeelint@1.5.1 -g"
    }
}
